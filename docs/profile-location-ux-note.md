# Profile location UX

Saved locations use a two-layer model:

1. Human-readable place details (label, address, barangay, city, province).
2. A fresh device GPS fix captured at save/update time.

Users never type coordinates manually. The UI should present this as a guided location setup flow, not a raw database form.
